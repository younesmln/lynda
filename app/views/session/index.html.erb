<% @page_title = "Admin Menu" %>

<div class="menu">
  <h2>Admin Menu</h2>
  <div class="identity">Logged in as: <%= current_user.username %></div>
  <ul>
    <li><%= link_to("Manage content", :controller => 'subjects') %></li>
    <li><%= link_to("Manage Admin Users", controller: 'users') %></li>
    <li><%= link_to("Logout", :action => 'logout') %></li>
  </ul>
</div>
